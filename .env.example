# Database Configuration
# PostgreSQL connection string for Neon or other PostgreSQL providers
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# These are automatically set by Neon/PostgreSQL providers but can be set manually if needed
PGHOST="your-postgres-host"
PGPORT="5432"
PGUSER="your-username"
PGPASSWORD="your-password"
PGDATABASE="your-database-name"

# Application Configuration
NODE_ENV="development"
PORT="5000"

# Optional: Add any API keys for external services here
# EXAMPLE_API_KEY="your-api-key-here"